/**
 * Retired guarantee placement retained as a no-op so existing imports remain
 * build-safe. No guarantee should render without a newly approved policy and
 * operations contract.
 */
export function GuaranteeBadge(_props: {
  className?: string;
  align?: "center" | "start";
}) {
  return null;
}
