/**
 * Retained as a no-op export for downstream imports during rollout.
 * Pricing urgency stays disabled until an approved effective date and
 * grandfathering policy exist. Billing behavior is intentionally unchanged.
 */
export function FoundingPricingBanner() {
  return null;
}
